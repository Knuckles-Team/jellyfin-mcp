{
  "mcpServers": {
    "jellyfin": {
      "command": "jellyfin-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "JELLYFIN_URL": "${JELLYFIN_URL:-http://localhost:8080}",
        "JELLYFIN_TOKEN": "${JELLYFIN_TOKEN}"
      }
    }
  }
}
